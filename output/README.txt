This folder will contain ranked_candidates.csv and ranked_candidates.json
after you run `python main.py` or use the Streamlit app.

The old output files were removed because they were generated before the
scoring bug fixes (education matching) and before jd.txt was replaced with
the Data Scientist role. Run the agent once to regenerate correct results.
